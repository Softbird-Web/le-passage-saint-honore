// Home page — sections assembled here, no logic
// Section order: Hero → Intro → Brunch → Terrasse → Privatisation teaser → Instagram → Contact

export default function HomePage() {
  return (
    <main>
      {/* sections added here as they are built */}
      <div className="flex items-center justify-center min-h-screen text-[var(--color-text-muted)] font-[var(--font-body)]">
        Passage Saint-Honoré — en cours de construction
      </div>
    </main>
  );
}
